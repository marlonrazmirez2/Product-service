spring:
  application:
    name: product-service
  cloud:
    config:
      uri: http://localhost:8084
      fail-fast: true
      retry:
            max-attempts: 3
            initial-interval: 1000